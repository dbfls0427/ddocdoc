<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ddocdoc.mapper.PresMapper">
	<cache />
	
	<!-- 처방전 등록 
	<insert id="hospitalInsert" parameterType="PresVO">
		insert into dd_pres values
		('P00' || pres_seq.nextval, '05/03/23', #{pres_count}, #{cus_num}, #{phar_num}, #{hos_num})
	</insert>
	
	고객 리스트
	<select id="customerList" resultType="CustomerVO">
		select * from dd_customer
	</select>
	
	-->
</mapper>