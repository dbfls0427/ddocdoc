<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ddocdoc.mapper.HospitalMapper">
	<cache />
	
	<!-- 병원 정보 등록 -->
	<insert id="hospitalInsert" parameterType="HospitalVO">
		insert into dd_hospital values('H00' || hospital_seq.nextval, #{hos_name}, #{hos_tel},
			#{hos_addr}, #{hos_time}, #{hos_info}, 0, #{hos_type})
	</insert>
	
	<!-- 병원 정보 상세 -->
	<select id="hospitalDetail" resultType="HospitalVO" parameterType="String">
		select * from dd_hospital 
		where hos_num = #{hos_num, jdbcType=VARCHAR}
	</select>
	
	<!-- 병원 정보 리스트 -->
	<select id="hospitalList" resultType="HospitalVO">
		select * from dd_hospital
	</select>
	
	
	<!-- 병원 정보 수정 -->
	<update id="hospitalUpdate" parameterType="HospitalVO">
		update dd_hospital 
		set  hos_name = #{hos_name}, hos_tel = #{hos_tel}, hos_addr = #{hos_addr}, hos_time = #{hos_time}, hos_info = #{hos_info}, hos_type = #{hos_type}
		where hos_num = #{hos_num}
	</update>
	
	<!-- 병원 정보 삭제 -->
	<delete id="hospitalDelete" parameterType="String">
		delete from dd_hospital 
		where hos_num = #{hos_num}
	</delete>
	
	
	<!-- 병원 예약 환자 리스트 -->
	<select id="hosResList" resultType="HospitalResVO" parameterType="String">
		select hos_res_num, hos_res_type, hos_res_sym, hos_res_memo, hos_res_date, hos_res_time, cus_num, hos_num, hos_res_acpt as hos_acpt
		from dd_hos_res
		where hos_num = #{hos_num, jdbcType=VARCHAR} 
	</select> 
	
	<!-- 예약 환자 이름 추출 
	<select id="hosResNameCustomer" parameterType="String">
		select b.cus_name
		from dd_hos_res a, dd_customer b
		where a.cus_num = b.#{cus_num}
	</select>
	-->
	
	
	<!-- 병원 예약 접수 -->
	<update id="booleanHosRes" parameterType="HospitalResVO">
		update dd_hos_res 
		set hos_res_acpt = '예약 접수 완료' 
		where hos_res_num = #{hos_res_num, jdbcType=VARCHAR}
	</update>
	
	
	<!-- 대기번호 증가 -->
	<update id="increaseWait" parameterType="String">
		update dd_hospital set hos_wait = hos_wait + 1
		where hos_num like #{hos_num}
	</update>  
	
	<!-- 대기번호 데이터 저장 -->
	<insert id="insertWaitData" parameterType = "HospitalWaitVO" > 
		insert into dd_res_wait values(#{cus_num}, #{hos_num}, #{res_wait} ,#{hos_res_num})
	</insert>
	
	<!-- 해당 병원 대기번호 발급 -->
	<select id="hospitalWait" parameterType="String" resultType="int" >
		select hos_wait from dd_hospital
		where hos_num = #{hos_num}
	</select>
	
</mapper>