<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="ddocdoc.mapper.CustomerMapper">
	<cache />
	
	<insert id="insertCustomer" parameterType="Customer">
	insert into dd_customer values('COO' || customer_seq.nextval, #{cus_id, jdbcType=VARCHAR}, #{cus_pw, jdbcType=VARCHAR}, #{cus_name, jdbcType=VARCHAR}, #{cus_addr, jdbcType=VARCHAR}, #{cus_email, jdbcType=VARCHAR}, '05/03/23')
	
	</insert>
	
	<select id="loginCustomer" parameterType="Login" resultType="Customer">
	select * from dd_customer where cus_id = #{loginId, jdbcType=VARCHAR} and cus_pw = #{loginPw, jdbcType=VARCHAR}
	
	
	</select>
	
	
</mapper>















